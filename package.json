{
  "name": "instabids-directus-setup",
  "version": "1.0.0",
  "description": "Setup scripts for Instabids Command Center",
  "scripts": {
    "setup": "node scripts/setup-directus.js",
    "setup:dev": "DIRECTUS_URL=http://localhost:8055 node scripts/setup-directus.js"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}
